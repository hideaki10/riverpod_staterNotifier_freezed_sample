name: riverpod_state_notifier_freezed_sample
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_riverpod: ^0.14.0+1
  freezed_annotation: ^0.14.1
  flutter_state_notifier: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic_mono: any
  build_runner:
  freezed:

flutter:
  uses-material-design: true
